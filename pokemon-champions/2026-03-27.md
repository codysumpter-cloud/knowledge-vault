# 2026-03-27

- `BeMore-stack/master` now includes the follow-up hardening from merged PR `#113`.
- Current task: harden the BMO operating environment so future Codex runs start from real BMO context instead of stale scaffolds.
- Direct donor lineage was confirmed:
  - `PrismBot` for policy and product patterns
  - `omni-bmo` for runtime and ops patterns
- Audit findings:
  - root startup files `memory.md`, `soul.md`, and `routines.md` were missing
  - canonical long-term memory file casing was inconsistent across docs and scripts
  - `TASK_STATE.md` and `WORK_IN_PROGRESS.md` still contained stale Linux-era checkpoint noise
- Follow-up hardening landed:
  - startup order now matches across `AGENTS.md`, `context/identity/AGENTS.md`, `context/RUNBOOK.md`, `context/BOOTSTRAP.md`, and `context/SESSION_STATE.md`
  - routine order now matches across `routines.md`, `docs/BMO_ROUTINES.md`, and `config/routines/bmo-core-routines.json`, with `worker-status` ahead of `worker-ready`
  - `skills/README.md` now matches the actual repo-local skill set and points to `skills/index.json`
  - validator coverage now fails on startup-order drift, routine-order drift, and missing skill coverage in the human-readable docs
- Commit `cf4b9ec` captured the BMO operating-environment hardening.
- Publishing the hardening required rebasing it onto newer `origin/master` continuity changes instead of pushing it directly.
