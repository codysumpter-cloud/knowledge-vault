# RUNBOOK

Canonical operational guidance lives in `context/RUNBOOK.md`.
Read that file instead of this root-level redirect.

Reason: the `context/` copy is the maintained source of truth and avoids drift between duplicated files.
